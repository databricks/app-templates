bundle:
  name: agent_non_conversational

resources:
  apps:
    agent_non_conversational:
      name: "agent-dhruv-offsite-demo"
      description: "Non-conversational agent application"
      budget_policy_id: "7da2ca5b-25b8-3e87-9a2c-f86eb19d101c"
      source_code_path: ./
      config:
        command: ["uv", "run", "start-server"]
        env:
          - name: MLFLOW_TRACKING_URI
            value: "databricks"
          - name: MLFLOW_REGISTRY_URI
            value: "databricks-uc"
          - name: MLFLOW_EXPERIMENT_ID
            value_from: "experiment"

      # Resources which this app has access to
      resources:
        - name: 'experiment'
          experiment:
            experiment_id: "2333333030218702"
            permission: 'CAN_MANAGE'

targets:
  dev:
    mode: development
    default: true
    # workspace:
    #   host: https://...

  prod:
    mode: production
    # workspace:
    #   host: https://...
    resources:
      apps:
        agent_non_conversational:
          name: agent-non-conversational
