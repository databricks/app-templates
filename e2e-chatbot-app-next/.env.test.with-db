# Test environment WITH DATABASE (persistent mode)
# This file is used by Playwright to test the application with PostgreSQL database enabled
#
# NOTE: Database configuration is inherited from .env.local
# This test mode does NOT define PG* variables - it expects them to already exist
# The test runner will verify isDatabaseAvailable() returns true before running tests

# Databricks configuration (mocked in tests via MSW)
DATABRICKS_SERVING_ENDPOINT=mock-value
DATABRICKS_CLIENT_ID=mock-value
DATABRICKS_CLIENT_SECRET=mock-value
DATABRICKS_HOST=mock-value

# Test mode marker - enables MSW mocking
PLAYWRIGHT=True
