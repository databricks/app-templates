/**
 * Export all routers for plugin-based architecture.
 * This allows the UI routes to be imported and used by the unified server.
 */

export { chatRouter } from './chat';
export { historyRouter } from './history';
export { sessionRouter } from './session';
export { messagesRouter } from './messages';
export { configRouter } from './config';
