# ============================================
# REQUIRED: Databricks Configuration
# ============================================
# First, run databricks auth login [--profile name]
# Then, set the profile name here (if you didn't specify a profile name, use "DEFAULT" as the profile name)
DATABRICKS_CONFIG_PROFILE=your-profile-name

# ============================================
# REQUIRED: PostgreSQL Configuration
# ============================================
# Option 1: Use individual PG* variables (recommended for Databricks authentication)
# This setup most-closely mimics the final setup when you deploy to Databricks apps,
# resulting in a higher-fidelity local dev environment
PGHOST=your-postgres-host # e.g. instance-000-111-abcd.database.cloud.databricks.com
PGUSER=your-username # e.g. user@mycompany.com
PGDATABASE=databricks_postgres # this is the default on Databricks
PGPORT=5432 # this is the default on Databricks

# Option 2: Use full connection string (alternative to PG* variables)
# POSTGRES_URL=postgresql://username:password@host:port/database
