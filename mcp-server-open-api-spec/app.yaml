command: ["uv", "run", "custom-open-api-spec-server"]

env:
  - name: "UC_CONNECTION_NAME"
    value: "" # TODO: Add the connection name
  - name: "SPEC_FILE_NAME"
    value: "spec.json"            # TODO: Update the spec file name if different from spec.json
  - name: "SPEC_VOLUME_PATH"
    valueFrom: "uc-volume"